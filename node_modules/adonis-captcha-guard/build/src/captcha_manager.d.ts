import type { HttpContext } from '@adonisjs/core/http';
import { CaptchaManagerFactory } from './types.js';
export declare class CaptchaManager<CaptchaProviders extends Record<string, CaptchaManagerFactory>> {
    #private;
    constructor(config: CaptchaProviders, ctx: HttpContext);
    /**
     * Returns the instance of a captcha provider
     */
    use<CaptchaProvider extends keyof CaptchaProviders>(provider: CaptchaProvider): ReturnType<CaptchaProviders[CaptchaProvider]>;
}
