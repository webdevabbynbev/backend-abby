import { RecaptchaConfig, RecaptchaValidateResult } from '../types.js';
import { Captcha } from './captcha.js';
import { HttpContext } from '@adonisjs/core/http';
/**
 * See more info about Google reCAPTCHA server-side validation: https://developers.google.com/recaptcha/docs/verify
 */
export declare class RecaptchaService extends Captcha {
    config: RecaptchaConfig;
    protected verifyUrl: string;
    protected tokenParamName: string;
    constructor(ctx: HttpContext, config: RecaptchaConfig);
    validate(): Promise<RecaptchaValidateResult>;
}
