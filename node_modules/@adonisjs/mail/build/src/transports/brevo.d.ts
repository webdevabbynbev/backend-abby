import { MailResponse } from '../mail_response.js';
import type { BrevoConfig, NodeMailerMessage, BrevoRuntimeConfig, BrevoSentMessageInfo, MailTransportContract } from '../types.js';
/**
 * Transport for sending emails using the Brevo `/emails/send` API.
 */
export declare class BrevoTransport implements MailTransportContract {
    #private;
    constructor(config: BrevoConfig);
    /**
     * Send message
     */
    send(message: NodeMailerMessage, config?: BrevoRuntimeConfig): Promise<MailResponse<BrevoSentMessageInfo>>;
}
