import { ResponseEnvelope } from './types.js';
/**
 * MailResponse represents a consistent response object returned
 * by all the mail transports
 */
export declare class MailResponse<T = undefined> {
    messageId: string;
    envelope: ResponseEnvelope;
    original: T;
    constructor(messageId: string, envelope: ResponseEnvelope, original: T);
}
